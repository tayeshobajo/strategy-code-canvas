Copy the uploaded skill from the archive into `.agents/skills/mockup-to-build-precision/` (SKILL.md, references/fidelity-rubric.md, agents/openai.yaml), then activate it via `skills--apply_draft` so it becomes available in the workspace.

No app code changes.