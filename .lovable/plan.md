Revert the global `--paper` design token in `src/styles.css` from the current warm cream back to the previous cool white (`oklch(0.985 0.005 85)`).

This single-line change will restore the cooler, less saturated `bg-paper` background across every page that uses it.